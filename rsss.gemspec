spec.author = 'Pablo Quiros'
spec.bindir = 'lib'
spec.default_executable = 'bin/rsss'
spec.add_dependency('rufus-scheduler', '>= 1.0.12')
spec.add_dependency('win32-service', '>= 0.6.1')
spec.summary = 'The Ruby Script Scheduler is a windows service script installer that installs any script to run in a previously defined schedule.'
spec.version = '1.0'
